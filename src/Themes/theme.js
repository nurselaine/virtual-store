import { createTheme } from "@mui/material";
import { palette } from "@mui/system";

export const theme = createTheme({
  palette: {
    primary: {
      light: '#9e9e9e',
      main: '#fff',
      dark: '#212121',
      contrastText: '#1769aa',
    },
  },
})